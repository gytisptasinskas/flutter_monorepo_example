name: dependency_injection
description: "A new Flutter package project."
version: 0.0.2
publish_to: 'none'

environment:
  sdk: ^3.7.2
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  get_it: ^8.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  linting:
    path: ../../utility/linting

