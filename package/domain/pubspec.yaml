name: domain
description: Shared domain logic for the application.
version: 0.0.1
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
environment:
  sdk: ^3.7.2
  flutter: ">=1.17.0"

dependencies:
  equatable: ^2.0.7
  flutter:
    sdk: flutter

dev_dependencies:

  flutter_test:
    sdk: flutter
  linting:
    path: ../../utility/linting

